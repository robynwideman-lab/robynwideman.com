# robynwideman.com

Static author site. Edit `data/books.json` (series, books, covers, links, copy), run `python3 build.py`, commit. Cloudflare Pages builds from this repo.

- Build command: `python3 build.py`
- Output directory: `public`
- Covers live in `assets/covers/`, free-book files in `assets/files/`.
